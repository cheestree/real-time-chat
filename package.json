{
  "name": "real-time-chat",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "src/client",
    "src/server"
  ],
  "scripts": {
    "build:shared": "npm run build --workspace=@rtchat/shared",
    "dev:shared": "npm run dev --workspace=@rtchat/shared",
    "dev:client": "npm run dev --workspace=src/client",
    "dev:server": "npm run dev --workspace=src/server"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
