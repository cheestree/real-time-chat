# Server Configuration
JWT_SECRET=demo_jwt_secret_change_in_production
CORS_ORIGIN=http://localhost:3000
SERVER_HOST=0.0.0.0
SERVER_PORT=4000
SERVER_PROFILE=dev
DEMO_MODE=true

# PostgreSQL Configuration
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=rtchat_demo
POSTGRES_USER=demo_user
POSTGRES_PASSWORD=demo_password

# MongoDB Configuration
MONGODB_USER=demo_mongo_user
MONGODB_PASSWORD=demo_mongo_password
MONGODB_DB=rtchat_demo
MONGODB_URI=mongodb://demo_mongo_user:demo_mongo_password@mongo:27017/rtchat_demo?authSource=admin

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=demo_redis_password

# Cassandra Configuration
CASSANDRA_HOST=cassandra
CASSANDRA_PORT=9042
CASSANDRA_CONTACT_POINTS=cassandra
CASSANDRA_KEYSPACE=rtchat
CASSANDRA_LOCAL_DATACENTER=datacenter1

# Client Configuration (browser-accessible URLs)
NEXT_PUBLIC_API_URL=http://localhost:4000/api
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000
